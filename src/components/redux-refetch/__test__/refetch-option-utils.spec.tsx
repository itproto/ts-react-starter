import { toTypedOptions } from '../refetch-option-utils';
describe('util classes for refetch HOC', () => {
  it('should parse different option input formats', () => {
    // toTypedOptions()
  });
});
